image:
  file: .gitpod.Dockerfile
tasks:
- name: Terminal
  command: >
    echo "";
    echo "Create aws workspace here: https://learn.acloud.guru/cloud-playground/cloud-sandboxes";
    cd iac/Lambda\ Proxy/;
    ./x_do_it_all.sh;
